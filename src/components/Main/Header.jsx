import styled from 'styled-components';

const Header = () => {
  return (
    <HeaderBlock>
      <div>
        <p>메인</p>
      </div>
    </HeaderBlock>
  );
};

const HeaderBlock = styled.div``;

export default Header;
